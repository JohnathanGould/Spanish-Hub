{
  "version": 2,
  "devCommand": "npm start",
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
